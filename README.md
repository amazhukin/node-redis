# Simple Redis using for caching with Node

Install:
```
npm install
```
Create `.env` file:
```
redisHost="********.cloud.redislabs.com"
redisPassword="************"
```

Run:
```
node server
```

Open in browser: http://localhost:3000/fish/red-snapper
